@using Rokhsetak.Resources
@using Microsoft.Extensions.Localization
@inject IStringLocalizer<SharedResourceMarker> L
@{
    ViewData["Title"] = L["Reg_MentorPending_Title"];
}

@section Styles {
    <link rel="stylesheet" href="~/css/forgotpassword.css" asp-append-version="true" />
}

<main class="forgot-wrapper" aria-label="@L["Reg_MentorPending_Aria"]">
    <article class="auth-card forgot-card" style="text-align:center;" aria-live="polite">
        <div class="auth-checkmark" aria-hidden="true">
            <i class="fa-solid fa-hourglass-half"></i>
        </div>

        <h1 class="auth-card__title" style="margin-bottom:10px;">@L["Reg_MentorPending_Title"]</h1>

        <p class="auth-card__subtitle">
            @L["Reg_MentorPending_Message_Part1"]
        </p>
        <p class="auth-card__subtitle">
            @L["Reg_MentorPending_Message_Part2"]
        </p>

        <a asp-controller="Home" asp-action="Index"
           class="auth-btn"
           style="display:inline-flex;align-items:center;justify-content:center;gap:8px;margin-top:16px;text-decoration:none;">
            <i class="fa-solid fa-house" aria-hidden="true"></i>
            @L["Reg_ReturnHome"]
        </a>
    </article>
</main>